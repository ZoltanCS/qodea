{
  "name": "@qodea/desktop",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "dev": "pnpm --filter @qodea/core build && pnpm build && electron . --dev",
    "start": "pnpm --filter @qodea/core build && pnpm build && electron ."
  },
  "dependencies": {
    "@qodea/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "electron": "^43.4.1",
    "typescript": "^7.0.2"
  }
}
