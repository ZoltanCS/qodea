# Normalize all text files to LF in the repo; git handles checkout per-OS.
* text=auto eol=lf

# Windows scripts need CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf
*.cmd text eol=crlf

# Binary assets
*.png binary
*.jpg binary
*.ico binary
*.icns binary
